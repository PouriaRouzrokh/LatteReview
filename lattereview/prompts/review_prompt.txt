Review the below article ${review_type}$ and reason if it meets the following criteria:

Article ${review_type}$: ${item}$

Review criteria: ${review_criteria}$

Now return a summary of your reasoning, and score the article using one of the following numbers: ${scoring_schema}$. Your scoring should be based on the following rules: ${scoring_rules}$

You can also ask a question ONLY AND IF ONLY the following criteria is met: ${question_critera}$

Your final output should have three keys: reasoning (string), scoring (a single integer following the above schema), and question (string; leave empty if no question).